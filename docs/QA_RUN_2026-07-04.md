# QA Checklist Run — 2026-07-04

Execution log for `docs/QA_CHECKLIST.md` Phases 13–20 (Sprint 30 — Final Feature Adjustment QA), recorded per its "Recording Results" section.

- **Date:** 2026-07-04
- **Environment:** Host-only (no Docker Desktop / MySQL container started this session) — backend suite runs against SQLite in-memory via `php artisan test`, frontend suite via Vitest/jsdom. No live browser session was used; all results below are automated-suite and static-code verification only.
- **Commit:** `b6bbe30` (`fix: enforce registration terms acceptance`) — no code changes made during this pass; QA found no regressions to fix.
- **Scope:** Phases 13–20 only (Sprints 20–29). Phases 0–12 (Sprints 1–19) were last executed live on 2026-07-03 (see `docs/QA_RUN_2026-07-03.md`) and were not re-run this session — nothing in Sprints 20–29 touched auth, roles, admin portal, time tracking, payroll, reports, or the original Dashboard/AI Insights foundations in a way that would invalidate that prior run.

## Automated Baseline

| Suite | Result |
| --- | --- |
| `composer validate` | **PASS** — `./composer.json is valid` |
| Backend `php artisan test` | **PASS** — 262/262 tests, 938 assertions |
| Frontend `npm run build` | **PASS** — known >500kB Recharts chunk warning only (pre-existing, documented) |
| Frontend `npm run lint` | **PASS** — zero warnings (oxlint) |
| Frontend `npm run test` | **PASS** — 258/258 tests, 48 files |

## Phase Results

| Phase | Result | Notes |
| --- | --- | --- |
| 13 — Home Dashboard & Attendance Widget | **PASS (automated + code spot-check)** | 11 `AttendanceTest.php` cases pass; `HomePage.test.tsx`/`AttendanceWidget.test.tsx` pass. Spot-check: grepped `PayrollController.php`, `HoursSummaryCalculator.php`, `PayrollFigures.php` for `AttendanceSession` — zero references confirmed; the `attendance_days` field that does appear there is Sprint 8's pre-existing, unrelated "days with time entries" concept. Browser click-through (does the widget visually update on Time In/Break/Time Out) not performed this session. |
| 14 — Notifications & Sidebar Badges | **PASS (automated + code spot-check)** | 6 `NotificationTest.php` + 5 `SidebarBadgeTest.php` cases pass; `NotificationCenter.test.tsx`/`NotificationsPage.test.tsx` pass. Spot-check: grepped `composer.json`/`package.json` for `reverb`/`pusher`/`soketi`/`socket.io` — no matches; polling-based approach confirmed unchanged. Live polling-interval timing and dropdown click-through not performed this session. |
| 15 — Profile Settings & Sidebar Profile | **PASS (automated)** | 10 `ProfileTest.php` cases pass, covering self-only update, password change (wrong-current-password rejection), and picture upload/view. Sidebar-reflects-update behavior not click-tested live this session. |
| 16 — KPI Redesign | **PASS (automated + code spot-check)** | `MyKpisPage.test.tsx`/`TeamKpisPage.test.tsx`/`admin/KpisPage.test.tsx`/`kpiInsights.test.ts` all pass. Spot-check: `admin/KpisPage.tsx` imports only `createKpi`/`listKpis` from `kpiApi` — confirmed no update/delete action exists, preserving the Sprint 25 catalog-only scope decision. |
| 17 — Team Timesheets Grouping & Analytics | **PASS (automated)** | `TeamTimesheetsPage.test.tsx`/`timesheetGrouping.test.ts` + backend `TeamHoursReportTrendTest.php` all pass, covering grouping/totals, filters, and all five dashboard-backed charts plus the Productivity Trend endpoint. |
| 18 — Team Scrum Kanban | **PASS (automated)** | `TeamScrumPage.test.tsx`/`scrumGrouping.test.ts` pass, covering per-employee grouping, collapse-by-default, and preserved comment behavior. |
| 19 — AI Assistant | **PASS (automated + code spot-check)** | Backend `AiAssistantTest.php` (13 cases, incl. every example question's classification, org-wide vs. department scoping, both flagged edge cases, and the generic-endpoint 404 guard) + frontend `AiInsightsPage.test.tsx`/`aiApi.test.ts` all pass. Spot-check: `visibleTabs`/`TAB_LABELS` logic in `AiInsightsPage.tsx` confirmed unchanged by the new assistant mode. |
| 20 — Registration Terms Fix | **PASS (automated)** | `RegisterPage.test.tsx` (7 cases, incl. the two new disabled-until-opened/disabled-until-accepted tests) + `Modal.test.tsx` pass. Full register→pending→approve→login path not re-executed live this session (last verified live on 2026-07-03, before the terms-modal change; the change only adds a client-side gate ahead of the existing, unchanged `POST /api/register` call). |

## Defects Found

None. No P0s, no regressions. Every phase's automated coverage passes against the current `main` branch, and the additional cross-cutting concerns called out in the draft checklist (attendance/payroll isolation, no realtime dependency, KPI catalog scope, AI Assistant tab isolation) were independently confirmed in code rather than only inferred from green tests.

## Outstanding (Not Performed This Session)

No Docker/MySQL environment or browser-automation tool was available this session, so the following remain manual follow-ups, consistent with every prior QA run's caveat:

- Live click-through of all eight new phases against a real running app (visual layout, chart rendering, modal appearance, expand/collapse interactions, polling timing).
- Full registration→approval→login live re-run with the new terms modal in the loop.
- A fresh `migrate:fresh --seed` + `DemoDataSeeder` run to confirm Sprints 20–29 behave correctly against MySQL (not just SQLite-in-memory) with realistic seeded data.
