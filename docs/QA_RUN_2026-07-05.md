# QA Run — 2026-07-05 (Sprint 42: Manual Browser QA and Runtime Verification)

Execution log for `docs/QA_CHECKLIST.md` Phase 21, recorded per its "Recording Results" section.

- **Date:** 2026-07-05
- **Environment:** Docker Compose stack — `postgres:16-alpine`, `redis:7-alpine`, `timeforge-app` (PHP 8.5-fpm + `pdo_pgsql`), nginx, Vite frontend. Fresh `migrate:fresh --seed` + `DemoDataSeeder` run at the start of this pass.
- **Commit:** working tree at the point of the Sprint 41 environment-consistency cleanup, plus this sprint's checklist/log additions (no application code changed this sprint).
- **Scope:** API/curl-level verification only — no browser-automation tool is available. This closes two real gaps (payroll/reports and the AI floating assistant had never been exercised against the live Postgres container before this pass) and re-confirms everything else already proven during the Sprint 40 regression pass still holds.

## Automated Baseline

| Suite | Result |
| --- | --- |
| `docker compose ps` | **PASS** — all 5 containers up |
| `docker compose exec app php artisan migrate:status` | **PASS** — 27/27 migrations `Ran` |
| `composer validate` | **PASS** |
| Backend `php artisan test` | **PASS** — 286/286 tests, 1002 assertions |
| Frontend `npm run build` | **PASS** — known >500kB Recharts chunk warning only |
| Frontend `npm run lint` | **PASS** — zero warnings |
| Frontend `npm run test` | **PASS** — 295/295 tests, 53 files |

## API/Curl Verification Results

| Item | Result | Evidence |
| --- | --- | --- |
| Login/logout | **PASS** | Token issued on login, correctly revoked on logout |
| Profile picture persistence | **PASS** | Upload → logout → fresh re-login (new token) → re-fetch returns correct image bytes |
| Department description CRUD/display | **PASS** | Admin update persists; public registration listing correctly omits description |
| Attendance full state machine | **PASS** | Time In → Pause (2nd Pause correctly rejected) → Resume → Time Out → 2nd Time In same day correctly rejected; `GET /attendance/today` read-back correct at every step |
| Registration OTP via `MAIL_MAILER=log` | **PASS** | Full chain: register → OTP found in log → verify → admin-visibility gate correct → approve → login |
| CAPTCHA gating (backend) | **PASS** | Missing token → 422; valid Cloudflare test-key token → passes |
| Notifications/sidebar badges | **PASS** | `GET /sidebar-counts` and `GET /notifications` agree on unread count |
| AI floating assistant (NEW this pass) | **PASS** | Supervisor question returns well-formed response (category `team_progress`, chart, table, recommendations); employee correctly forbidden with 403 |
| File uploads/downloads | **PASS** | Both profile pictures and time entry attachments — upload/download byte-correct, delete confirmed |
| Payroll/reports (NEW this pass) | **PASS** | `/api/payroll` returns correct seeded figures (Eve: $20/hr, 480 approved minutes, $160 estimated); PDF export confirmed genuinely valid (`/Count 1` in the file's own object structure — `file` utility's "0 pages" report was a false alarm, verified via `strings`); Excel export valid `.xlsx` |
| No MySQL missing-table errors | **PASS** | Fresh `migrate:fresh --seed` + `DemoDataSeeder` both completed with zero errors; every endpoint above returned real data |

**No defects found.** One false alarm investigated and ruled out: the `file` command reported the payroll PDF export as "0 pages," which would have been a real P1 if true — direct inspection of the PDF's own object structure (`/Count 1`, valid `/Type /Page` objects) confirmed this was a limitation of `file`'s heuristic parsing of this dompdf-generated PDF, not an actual defect.

## Browser Checks Still Needed

Not performed this session — no browser-automation tool available. The nine-item numbered checklist in `docs/QA_CHECKLIST.md` Phase 21 lists the exact manual steps. Highest priority: **CAPTCHA widget rendering** (item 6) — this is the one item that fundamentally cannot be verified any other way, since the Turnstile widget is client-side JS loaded from Cloudflare.

## Conclusion

**Overall: PASS.** All API/curl-verifiable scope items for Sprint 42 pass with no regressions and no defects. Two real verification gaps from prior sprints (payroll/reports, AI assistant against Postgres) are now closed. Browser-only verification remains the one outstanding category, tracked as an explicit, executable checklist rather than a vague caveat.
