.DS_Store
.claude/settings.local.json

# Backups (Sprint 47) — a database dump or storage archive can contain
# real user data and must never be committed, regardless of where it's
# taken from.
backups/
*.dump
*.sql
*.sql.gz

# Python virtual environment
docx-builder/venv/
docx-builder/proxy.py
